<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.kirkbushman.sampleapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:allowBackup="true"
            android:name=".TestApplication"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"></activity>
        <activity android:name=".activities.SelfAccountActivity"></activity>
        <activity android:name=".activities.SelfAccountInfoActivity"></activity>
        <activity android:name=".activities.InboxActivity"></activity>
        <activity android:name=".activities.RedditorActivity"></activity>
        <activity android:name=".activities.RedditorInfoActivity"></activity>
        <activity android:name=".activities.SubredditActivity"></activity>
        <activity android:name=".activities.SubmissionActivity"></activity>
        <activity android:name=".activities.SubmissionsActivity"></activity>
        <activity android:name=".activities.CommonSubmissionsActivity"></activity>
        <activity android:name=".activities.CommentActivity"></activity>
        <activity android:name=".activities.CommentsActivity"></activity>
        <activity android:name=".activities.WikiPageActivity"></activity>
        <activity android:name=".activities.RulesActivity"></activity>
        <activity android:name=".activities.SubscribedSubredditsActivity"></activity>
        <activity android:name=".activities.SelfTrophiesActivity"></activity>
        <activity android:name=".activities.UserTrophiesActivity"></activity>
    </application>

</manifest>